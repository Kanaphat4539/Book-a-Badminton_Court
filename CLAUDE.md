# แนวทางทำงานร่วมกัน

## แหล่งข้อกำหนดหลัก

อ่าน `feedback.md` ที่ root ก่อนเริ่มงานทุกครั้ง ใช้กติกาที่ตกลงแล้ว, Feature ID (`Axx`, `Uxx`, `Sxx`), ขอบเขตไฟล์ และเกณฑ์ตรวจรับจากไฟล์นั้นเป็นหลัก ไม่คัดลอกกติกาธุรกิจมาเก็บซ้ำในเอกสารนี้ เพื่อป้องกันข้อมูลไม่ตรงกัน

- คำสั่งล่าสุดที่ชัดเจนจากผู้ใช้มีผลเหนือข้อเสนอเดิม หากเปลี่ยน requirement ให้ระบุความต่างและปรับเอกสารเมื่ออยู่ในขอบเขตงานที่ได้รับอนุญาต
- การแบ่งฝั่งยืนยันแล้ว: MIDTION รับ User และ Nongpooh รับ Admin ส่วนเจ้าของงานส่วนกลางใน feedback ยังเป็นข้อเสนอ ให้ยึดการมอบหมายในงาน/PR หรือคำยืนยันของทีมก่อนแก้ส่วนกลาง
- ประเด็นในหัวข้อ “รายละเอียดที่ยังต้องยืนยัน” ห้ามเดากติกาที่มีผลต่อสิทธิ์ผู้ใช้หรือผลการจอง ถามเฉพาะประเด็นที่ขวางงานปัจจุบัน และทำส่วนที่ไม่ขึ้นกับคำตอบต่อได้
- การขอตรวจสอบหรือวางแผนไม่ใช่การอนุญาตให้แก้โค้ด หากได้รับคำสั่งให้แก้แล้ว ให้ทำในขอบเขตนั้นได้โดยไม่ถามซ้ำทุกขั้นตอน
- อ่าน `AGENTS.md` / `CLAUDE.md` เพิ่มเติมของโฟลเดอร์ที่จะแก้ สำหรับ frontend มี `frontend/CLAUDE.md` และ `frontend/AGENTS.md`; ปฏิบัติตามคำแนะนำให้อ่านเอกสาร Next.js ที่ติดตั้งในโปรเจกต์ก่อนแก้โค้ดที่เกี่ยวข้อง

## เริ่มงาน

1. อ่าน feedback และคำสั่งผู้ใช้ ระบุ Feature ID ที่รับผิดชอบ หากเป็นงานใหม่ที่ยังไม่มี ID ให้ระบุขอบเขตอย่างชัดเจนก่อน
2. ตรวจ `git status --short` และ `git branch --show-current` ก่อนแก้ไฟล์ รักษาการเปลี่ยนแปลงเดิมของผู้อื่น จากนั้นนำ `dev` ล่าสุดเข้า branch ของตนก่อนเริ่มงานทุกครั้ง ตามขั้นตอนด้านล่าง
3. ตรวจ implementation จริง อย่าสรุปว่า feature เสียหรือไม่มีเพียงเพราะอยู่ในรายการ feedback
4. แจ้งสั้น ๆ ว่าจะทำ Feature ID ใด ไฟล์ใด และมี dependency ต่ออีกฝั่งหรือไม่ ไม่ต้องสร้างเอกสารแผนใหม่สำหรับงานเล็ก
5. ถ้าไฟล์ร่วมยังไม่มีเจ้าของ ให้เคลียร์เจ้าของก่อนแก้ไฟล์นั้น ไม่ต้องหยุดงานอื่นที่แยกทำได้

## แบ่ง branch และเจ้าของไฟล์

- ใช้ branch ที่ทีมสร้างแล้ว: Nongpooh ทำงานบน `feature-nongpooh` และ MIDTION ทำงานบน `feature-midtion` โดยมี `dev` เป็น branch รวมงาน ไม่ลง implementation โดยตรงบน `dev`
- MIDTION รับ Feature ฝั่ง User (`U01`–`U10`) บน `feature-midtion`; Nongpooh รับ Feature ฝั่ง Admin (`A01`–`A09`) บน `feature-nongpooh` งานส่วนกลาง (`Sxx`) ต้องยืนยันเจ้าของแยกจากการแบ่งฝั่งนี้
- แต่ละคน/agent ต้องใช้คนละ checkout หรือ worktree เมื่อทำงานพร้อมกัน ห้ามสลับ branch ใน working directory เดียวกันที่อีกคนกำลังใช้
- ยึดตารางขอบเขตไฟล์ใน feedback เจ้าของ Admin/User ไม่ได้มีสิทธิ์แก้ไฟล์ร่วมทุกไฟล์โดยอัตโนมัติ
- `MainLayout`, root layout, global styles, theme, UI components, Dashboard, API client, backend และ schema ต้องมีเจ้าของที่ชัดเจน
- หาก Dashboard รวมทั้งสองบทบาท ให้เจ้าของแยก component ใน PR ส่วนกลางก่อน หรือให้เจ้าของคนเดียวรวมการเปลี่ยนแปลง ห้ามแก้ส่วนเดียวกันสอง branch โดยไม่ประสาน
- หากจำเป็นต้องแตะไฟล์ของอีกฝั่ง ให้ระบุไฟล์ เหตุผล และการเปลี่ยนแปลงที่ต้องการ ส่งมอบผ่านรายละเอียดงาน/PR ให้เจ้าของจัดการ หรือรับมอบหมายขอบเขตเพิ่มก่อน
- dependencies, lockfiles และ config เปลี่ยนเฉพาะที่งานจำเป็น ห้าม format/refactor ทั้งโปรเจกต์พ่วงมากับ feature
- `feedback.md` และ `CLAUDE.md` เป็นเอกสารร่วม ให้แก้ผ่านงานเอกสารที่ตกลงกัน ไม่ให้ทุก branch ติ๊กสถานะหรือเขียนรายงานลงไฟล์เดียวกัน ใช้ PR รายงานความคืบหน้าแทน

## ข้อตกลงระหว่าง Admin / User / Backend

- ก่อนทำงานเชื่อมกัน ให้ตกลง API ที่เกี่ยวข้อง: endpoint/method, request/response, role ที่เรียกได้, สถานะและรหัสข้อผิดพลาด, รูปแบบเวลาและ timezone บันทึกในรายละเอียด PR ส่วนกลางหรือเอกสาร API เดิมที่ทีมใช้
- อย่าสร้าง endpoint หรือชื่อสถานะคนละชุดในสอง branch ตรวจของเดิมและข้อตกลงก่อน
- Backend เป็นผู้บังคับกติกาการจอง การยกเลิก เช็กอิน โควตาและแบน UI มีหน้าที่แสดงและช่วยตรวจเบื้องต้น ไม่ใช้สถานะใน browser เป็นหลักฐานอนุญาตสิทธิ์
- ป้องกันคำขอพร้อมกันและการประมวลผลซ้ำ โดยเฉพาะจองคอร์ต นับความผิด และงานยกเลิกอัตโนมัติ
- งานตามเวลาและปลดแบนต้องทำงานได้แม้ผู้ใช้ไม่เปิดหน้าเว็บ ใช้เวลาที่ระบบตกลงกัน ไม่ตั้งกติกา timezone ขึ้นเอง
- เอาหน้าจอง/Scan ออกจาก Admin ต้องไม่ทำให้ User ใช้ไม่ได้ และยังต้องมีจุดแสดง QR ของ Admin
- หาก API ยังไม่พร้อม สามารถทำ UI ด้วย mock ตาม contract ที่ตกลงแล้วได้ แต่แยก mock ให้ชัด และห้ามรายงานว่า integration ผ่านจนกว่าจะทดสอบกับ backend จริง
- การเปลี่ยน contract/schema ที่กระทบอีกฝั่ง ให้รวมเป็น PR ส่วนกลางและอัปเดตทั้งสอง branch ก่อนพัฒนาต่อ

## ตรวจงาน

เลือกตรวจตามความเสี่ยงของสิ่งที่แก้ และใช้ checklist ใน feedback สำหรับ flow ร่วม ทดสอบขอบเวลา 15 นาที โควตารายวัน การยกเลิกแล้วจองใหม่ ความผิดสะสมและแบน รวมถึงคำขอพร้อมกันเมื่อแก้กติกาเหล่านี้

คำสั่งต่อไปนี้อ้างอิง scripts ปัจจุบัน ให้ตรวจ package.json อีกครั้งหากมีการเปลี่ยนแปลง รันจากโฟลเดอร์ที่ระบุ:

| โฟลเดอร์ | คำสั่ง | ใช้ตรวจ |
| --- | --- | --- |
| `frontend` | `npm run lint` | ESLint |
| `frontend` | `npm run build` | Production build และการตรวจที่ build ทำให้ |
| `backend` | `npm test -- --runInBand` | Unit tests; จำกัด test ที่เกี่ยวข้องได้ระหว่างพัฒนา |
| `backend` | `npm run build` | Backend build |
| `backend` | `npm run test:e2e -- --runInBand` | Integration/E2E เมื่อแก้ flow/API ที่เกี่ยวข้องและมี test environment พร้อม |

- `backend` มี `npm run lint` ที่ใช้ `--fix` และ `npm run format` ที่เขียนไฟล์ อย่าใช้เป็น read-only check หรือรันแล้วปล่อยให้แก้ไฟล์นอกขอบเขตโดยไม่ตรวจ diff
- งาน UI ให้ตรวจหน้าจอจริงและ interaction ตามที่เปลี่ยน รวมบทบาทและธีมที่เกี่ยวข้อง ไม่ถือว่า build ผ่านเท่ากับ UI ผ่าน
- งานเอกสารอย่างเดียวให้ตรวจความตรงกับ feedback และ diff ไม่จำเป็นต้อง build/test แอป
- ห้ามใช้ข้อมูลจริงเพื่อทดสอบการยกเลิก/แบน ใช้ข้อมูลและฐานข้อมูลทดสอบแยก
- รายงานคำสั่งที่รัน ผลจริง และสิ่งที่ยังไม่ได้ตรวจ ถ้ามี failure เดิมหรือ environment ขวาง ให้แยกออกจาก regression ที่เกิดจากงานนี้

## อัปเดตจาก dev ก่อนเริ่มงานทุกครั้ง

ทำใน checkout ของตน ตรวจว่างานค้างถูกเก็บอย่างเหมาะสมก่อน pull หากมีงานของผู้อื่นหรือไม่แน่ใจว่าจะเก็บอย่างไร ให้แจ้งก่อน ห้าม reset ทิ้งงานหรือ stash งานของผู้อื่นเอง

ตัวอย่างสำหรับ MIDTION (Nongpooh เปลี่ยนชื่อ branch เป็น `feature-nongpooh`):

```bash
git status --short
git switch feature-midtion
git pull --no-rebase origin dev
```

คำสั่งนี้นำ `origin/dev` มารวมใน feature branch ที่กำลังทำงาน โดยเลือก merge อย่างชัดเจน ไม่เปลี่ยน upstream ของ feature branch การ pull เฉพาะ local `dev` แล้วกลับ feature branch โดยไม่ merge ยังไม่ทำให้ feature branch อัปเดต หากเกิด conflict ให้แก้และตรวจผลให้เสร็จก่อนเริ่ม feature ใหม่ หาก pull ไม่สำเร็จให้รายงาน ห้ามอ้างว่าซิงก์แล้ว

## ส่งงานและรวมเข้า dev

1. รวมเอกสารข้อตกลงและ PR ส่วนกลางที่จำเป็นเข้า `dev` ก่อนงานที่พึ่งพา
2. ก่อนเริ่มงานทุกครั้ง และก่อนรวมงาน ให้อัปเดต `feature-nongpooh` หรือ `feature-midtion` จาก `dev` ตามขั้นตอนด้านบน ห้าม reset ทิ้งงานหรือ force-push branch ที่แชร์เอง
3. ตรวจ diff และ stage เฉพาะไฟล์ในงาน อย่ารวม `.env`, secrets, ฐานข้อมูล runtime, build output หรือการแก้ของคนอื่น
4. เมื่อทำเสร็จ ให้ commit เฉพาะงานของตนและ push feature branch ของตน เป้าหมายสุดท้ายคือรวมเข้า `dev` วิธีที่แนะนำคือเปิด PR จาก feature branch เข้า `dev` ทีละขอบเขต พร้อมระบุ dependency; การ push feature branch อย่างเดียวยังไม่ได้รวมงานเข้า `dev`
5. Merge ทีละ PR หลังตรวจรับ อีก branch อัปเดตจาก `dev` แล้วทดสอบ flow ที่เชื่อมกันอีกครั้ง
6. หาก conflict กระทบกติกาหรือ contract ให้ตรวจเจตนาของทั้งสองฝั่ง ห้ามเลือก ours/theirs ทั้งไฟล์เพียงเพื่อให้ merge ผ่าน

หากทีมเลือก merge และ push `dev` โดยตรงแทน PR ให้ผู้รวมงานทำทีละคน: อัปเดต local `dev` ด้วย `git pull --ff-only origin dev`, merge feature branch ที่ตรวจแล้ว, ตรวจผลรวม แล้ว `git push origin dev` หาก push ถูกปฏิเสธเพราะมีงานใหม่ ให้ดึงและรวมงานล่าสุดพร้อมตรวจใหม่ ห้าม force-push ข้ามงานอีกคน และห้ามข้าม branch protection หาก remote บังคับ PR

การสั่งแก้โค้ดไม่หมายถึงอนุญาต push, เปิด PR หรือ merge โดยอัตโนมัติ ทำเมื่อผู้ใช้มอบหมายไว้แล้ว ห้ามอ้างว่าทำขั้นตอนเหล่านี้เสร็จหากยังไม่ได้ทำ

### รูปแบบส่งมอบงาน / PR

- Feature ID และปัญหาที่แก้
- พฤติกรรมหลังแก้และไฟล์สำคัญ
- API/schema/shared files ที่เปลี่ยน และ dependency ต่ออีก branch
- การทดสอบที่ทำพร้อมผล และข้อจำกัดที่ยังเหลือ
- ประเด็นที่อีกคนต้องทราบก่อนอัปเดต branch หรือ merge

สื่อสารเป็นภาษาไทย กระชับ ระบุหลักฐานและข้อจำกัดให้ชัด อย่าอ้างว่าการแบ่ง branch รับประกันว่าจะไม่เกิด merge conflict
