# Test Checklist — ระบบจองสนามแบดมินตัน

วันที่จัดทำ: 19 กันยายน 2026

เอกสารนี้เป็นแผนทดสอบพร้อมผลรอบ QA-20260919 สำหรับหมวด 1–3 เท่านั้น หมวดอื่นยังไม่ได้ทดสอบในรอบนี้ ช่องที่ติ๊กผ่านมีหลักฐานตามตารางด้านล่าง ไม่ได้หมายความว่าระบบทั้งหมดผ่านแล้ว

## ผลทดสอบรอบ QA-20260919 — หมวด 1–3

ผู้ทดสอบ: TAMAYO (automated API/integration tests) | วันที่: 19 กันยายน 2026 | Commit: `1e4eba2` พร้อม working-tree fixes ของ session ที่มีอยู่ก่อนรอบนี้

**สรุป 32 รายการ: ผ่าน 12, ไม่ผ่าน 6, ยังยืนยันครบไม่ได้/Blocked 14** ไม่มีการแก้ production code ในรอบทดสอบนี้

Environment: Windows, Node.js v26.4.0, backend build ใหม่จาก source ปัจจุบัน, NestJS app จริง + Supertest + repositories/transactions จริงบน SQL.js (SQLite ในหน่วยความจำ) ไม่ได้เชื่อมต่อหรือแก้ข้อมูล MySQL/บัญชีใช้งานจริง ทดสอบเวลาไทยด้วยนาฬิกาจำลอง เริ่ม `2026-09-19 10:00 +07:00` และเรียก Cron handler จริงโดยตรงหลังหยุด scheduler เพื่อให้ทำซ้ำได้ จึงยังไม่ยืนยันความถี่ scheduler จริงทุกนาที ผลเรื่องความยาว field/ตัวพิมพ์ใหญ่เล็กต้องทดสอบ MySQL ซ้ำเพราะข้อจำกัด driver/collation ต่างกัน

หลักฐาน: [สคริปต์ทดสอบ](backend/test/checklist-sections-1-3.cjs) และ [ผล API รายกรณี](backend/test/checklist-sections-1-3-results.json) — ค่า PASS ใน JSON เป็นผลของ subtest ฝั่ง API เท่านั้น; สถานะระดับ checklist ให้ยึดตารางนี้ ซึ่งแยกส่วน UI ที่ยังไม่ได้ทดสอบไว้แล้ว ไม่บันทึก JWT หรือ password hash ในหลักฐาน

ข้อจำกัดเครื่องมือ: Browser runtime เริ่มไม่ได้ด้วย `The system cannot find the path specified. (os error 3)` จึงยังไม่มี browser/device/screenshot evidence และยังไม่ได้รับคำตอบอนุญาต fallback ไป Playwright ณ เวลาบันทึกผล ห้ามนับ API ผ่านแทน UI/กล้องที่ยังไม่ได้ตรวจ

| Test ID | ผลรอบนี้ | หลักฐาน / ปัญหา / ส่วนที่ยังต้องตรวจ |
| --- | --- | --- |
| SET-01 | BLOCKED | ยังไม่ได้เปิด frontend เชื่อม isolated backend ผ่าน `/api`; การทดสอบนี้ใช้ Supertest ต่อ Nest โดยตรง ไม่ยืนยัน rewrite/environment ของหน้าเว็บ |
| SET-02 | PASS | สร้าง schema/seed ได้; เรียก seed ซ้ำและสร้าง Nest app ใหม่จาก DB export แล้วจำนวนบัญชีเดิมไม่เพิ่มซ้ำ |
| SET-03 | PASS | นักศึกษา A/B คนละ ID, admin login ได้ ADMIN; Guest เรียก `/bookings/me` ได้ 401 |
| SET-04 | PASS | สร้าง/readback fixture strikes 0/1/2 และ banned_until ก่อน/หลังเวลาทดสอบตรงกัน |
| SET-05 | PASS | เตรียม 8 แถว: 4 สถานะ × วันนี้/เมื่อวาน และ created_at=null; ล้าง fixture ก่อนเคสถัดไป |
| SET-06 | PASS | availability ตรงกับ fixture 0, 1, 4, 60 ช่อง; ชุดเต็มเป็นข้อมูลจำลองความจุที่สร้างตรงใน DB ไม่ใช่ให้คนเดียวจองผ่าน API |
| SET-07 | PASS | ควบคุมเวลาไทยได้; deadline 10:15:00 ปฏิเสธ check-in, ย้อนเป็น 10:14:59 ยอมรับ |
| SET-08 | PASS | export DB ในหน่วยความจำ, ล้าง bookings, import กลับ แล้ว bookings/จำนวนผู้ใช้ตรงเดิม; ไม่ใช่ผล restore production backup |
| SMK-01 | BLOCKED — API ผ่าน | register ได้ 201 และตัวตนถูกต้อง; ยังไม่ยืนยันหน้า Dashboard หลังสมัคร |
| SMK-02 | BLOCKED — API ผ่าน | login/create ผ่านและ DB มี PENDING เดียว; ยังไม่กดเลือกวัน/เวลา/สนามและ dialog จริง |
| SMK-03 | BLOCKED — API ผ่าน | admin อ่านรายการได้; เจ้าของ check-in ผ่าน API แล้ว GET me เป็น CHECKED_IN; ยังไม่ได้สแกน QR/ยืนยันสองหน้าจอ |
| SMK-04 | PASS (integration) | เลื่อนเวลาถึง 11:00 แล้วเรียก Cron handler จริง: CHECKED_IN → COMPLETED และประวัติยังอยู่; ยังไม่ใช่การรอ scheduler ตามเวลาจริง |
| SMK-05 | BLOCKED — API ผ่าน | cancel แล้ว strikes=0 และ B จองช่องเดิมได้; ยังไม่กดยืนยันยกเลิกบน UI |
| SMK-06 | PASS (integration) | no-show เกิน deadline แล้ว Cron ยกเลิก/เพิ่ม 1 strike; เรียกซ้ำไม่เพิ่มซ้ำ |
| SMK-07 | PASS (integration) | ผิดกฎสองครั้งได้ 2 strikes, แบนอีก 86,400,000 ms, จองได้ 400 ระหว่างแบน; ครบ 24h ปลดแบนและจองวันใหม่ได้ 201 |
| SMK-08 | BLOCKED | ยังไม่ทดสอบปุ่ม logout, Back และ direct protected URL บน browser |
| REG-01 | BLOCKED — API ผ่าน | สมัครด้วยข้อมูลถูกต้องได้ 201 และชื่อ/email/เบอร์/major/year ตรง DB; ยังไม่ยืนยัน form/หน้าปลายทาง |
| REG-02 | FAIL — QA-01 | API รับ name/phone/major/year/username/password ว่างและสร้างบัญชีได้ 201; studentId ว่างได้ 500; email ว่างปฏิเสธ 400 ถูกต้อง |
| REG-03 | PASS (API) | `qa@gmail.com` และ `qa@kmitl.ac.th.evil.test` ได้ 400 |
| REG-04 | FAIL — QA-02 | `@kmitl.ac.th`, `qa space@kmitl.ac.th`, `qa@@kmitl.ac.th` สมัครผ่าน API ได้ 201 |
| REG-05 | PASS (API) | username ซ้ำกับ STUDENT/ADMIN ได้ 400 `Username already exists` |
| REG-06 | FAIL — QA-03, QA-04 | studentId ซ้ำได้ 201 และเปลี่ยน username/email/password ของบัญชีเดิม; email ซ้ำทดสอบบน fixture แยกแล้วได้ 500 |
| REG-07 | FAIL — QA-05 | studentId ว่างได้ 500; `123456789` และ `abcdefgh` ได้ 201 และมีแถวใน SQLite; ยังไม่ยืนยัน MySQL behavior |
| REG-08 | BLOCKED — API ผ่าน | response เก็บ/คืนชื่อไทย, หลายคำ, trim หัวท้ายถูกต้อง; ยังไม่ได้ดูชื่อที่ render บน UI |
| REG-09 | FAIL — QA-06 | เบอร์ 16 ตัวได้ 201 ใน SQLite; year=`abc` ได้ 201 และถูกแปลงเป็น 1 |
| REG-10 | BLOCKED — รอกติกา | ทดลองได้ 201 ทุกค่า: 0 ถูกแปลงเป็น 1, -1 เก็บ -1, 1.5 ถูกตัดเป็น 1, 99 เก็บ 99; ยังไม่มีช่วงปีที่ยืนยันแล้ว |
| REG-11 | FAIL — QA-07 | password ว่าง/ไม่ส่งได้ 201 และล็อกอินด้วยรหัสผ่านเริ่มต้น `password` ได้ 201 |
| REG-12 | BLOCKED — รอกติกา | ทดลองรหัสผ่านยาว 1 และ 100 ตัว API รับทั้งคู่; ยังไม่มีกติกาความยาว/อักขระที่ตกลงและยังไม่ได้ทดสอบ UI |
| REG-13 | BLOCKED — รอกติกา | ใน SQLite: username เดิมล็อกอิน 201, เปลี่ยน case/เติมช่องว่างได้ 401; ยังไม่ยืนยันนโยบายและ collation ของ MySQL |
| REG-14 | BLOCKED — API ผ่านบางส่วน | ส่ง register เหมือนกันพร้อมกันสองคำขอได้ 201 ทั้งคู่ แต่ DB มีบัญชีเดียว ไม่มี 500; ยังไม่ทดสอบปุ่มสมัครรัว/Enter บน form และยังไม่กำหนด contract การตอบ request ซ้ำ |
| REG-15 | BLOCKED | ยังไม่ได้จำลอง network failure ขณะกรอก form จึงไม่ยืนยันการรักษาค่าในช่อง/การ retry |
| REG-16 | BLOCKED — API ผ่าน | register แล้ว login ด้วยรหัสเดิมได้ 201; auth response ไม่มี password/hash; ยังไม่ทดสอบออกจากบัญชีแล้วเข้าใหม่ผ่าน UI |

### ปัญหาที่พบและขั้นตอนทำซ้ำ

ใช้ payload ตั้งต้นที่ถูกต้องและไม่ซ้ำ: studentId 8 หลัก, email ของชุด QA `@kmitl.ac.th`, name, phone, major, year=`3`, username และรหัสผ่านทดสอบครบทุกช่อง เปลี่ยนเฉพาะ field ของเคสที่ระบุ ยิง `POST /auth/register` บนฐานทดสอบเท่านั้น

| Bug ID | ระดับเบื้องต้น | วิธีทำซ้ำ / ผลจริง | ผลที่ควรได้ |
| --- | --- | --- | --- |
| QA-01 | High | ทำ payload ใหม่แต่ละครั้งแล้วตั้ง name/phone/major/year/username/password เป็นข้อความว่างทีละช่อง → 201; studentId ว่าง → 500 | validation 4xx ที่สื่อสารได้และไม่สร้างบัญชีไม่สมบูรณ์; email ว่างผ่านการปฏิเสธแล้ว |
| QA-02 | Medium | เปลี่ยน email เป็น `@kmitl.ac.th`, `qa space@kmitl.ac.th`, `qa@@kmitl.ac.th` → 201 ทุกค่า | ตรวจโครงสร้าง email ไม่ใช่แค่ suffix และตอบ 4xx |
| QA-03 | Critical | สมัคร A ให้สำเร็จ; สมัคร B ด้วย studentId ของ A แต่ใช้ username/email ใหม่ → 201; readback ID เดิมพบข้อมูล B เขียนทับ A | ปฏิเสธรหัสนักศึกษาซ้ำและบัญชี A ต้องไม่เปลี่ยน; เป็นปัญหาความถูกต้อง/ความปลอดภัยของบัญชีที่ยืนยันใน isolated DB |
| QA-04 | Medium | สมัครด้วย email ที่มีอยู่แล้ว โดย studentId/username ใหม่ → 500 `Internal server error` | controlled duplicate validation 4xx และข้อความเข้าใจได้ |
| QA-05 | Medium | studentId=`123456789`/`abcdefgh` → 201 และบันทึก; ว่าง → 500 | ตรวจ length/รูปแบบตามนโยบายและไม่เกิด 500; ความยาวที่ DB ยอมรับเป็นผลบน SQLite ต้องยืนยัน MySQL ซ้ำ |
| QA-06 | Medium | phone 16 ตัว → 201; year=`abc` → 201 และ DB เก็บ 1 | validation 4xx ไม่แปลง input ผิดเป็นค่าที่ดูถูกต้องโดยเงียบ |
| QA-07 | High | สมัครโดย password ว่างหรือไม่ใส่ field → 201; login username เดิมด้วย `password` → 201 | ปฏิเสธการสมัครที่ไม่มี password ไม่แจก default password โดยไม่ตั้งใจ |

### วิธีรันหลักฐานซ้ำและข้อจำกัด

Dependency QA `sql.js` ติดตั้งแยกใน `.qa-deps/node_modules` และไม่ได้เปลี่ยน package.json/package-lock.json ของแอป โฟลเดอร์ node_modules ถูก ignore ตามเดิม รันจาก root:

```powershell
npm.cmd install --prefix .qa-deps --no-save --package-lock=false sql.js
cd backend
node node_modules/@nestjs/cli/bin/nest.js build
node test/checklist-sections-1-3.cjs
```

สคริปต์ตั้ง DataSource แยกด้วย Nest testing provider, ใช้ entities/services/controllers จริง และปิดแอปเมื่อจบ; ไม่เปิด backend พอร์ต production ผล JSON มีทุก subcase รวมค่าที่ทดลองแต่ยังไม่มีกติกา (`OBSERVED`) สคริปต์เป็น diagnostic runner ที่เก็บหลาย failure ต่อรอบ ต้องอ่าน `status` ใน JSON ไม่ใช้ process exit code เป็นเกณฑ์ว่าทุกเคสผ่าน

ปัญหาระหว่างเตรียมชุดทดสอบ: driver ชื่อ `sqlite` ไม่มีใน TypeORM ที่ติดตั้ง และไม่มี `better-sqlite3`; จึงใช้ SQL.js ผ่าน test provider เท่านั้น ไม่ได้เปลี่ยน config แอป ผล 500 ที่เคยเกิดจากการเปิด Nest สองแอปใน process เดียวแล้วปิด Passport strategy ของแอปที่สองถูกแยกเป็นปัญหา harness และแก้ลำดับทดสอบแล้ว ผลใน JSON เป็นรอบสุดท้ายหลังแก้ harness ไม่ได้นำ failure นั้นมารายงานเป็น bug ของผลิตภัณฑ์

**ข้อสรุปรอบนี้:** ยังไม่ควรรับรองสามหมวดว่าผ่านครบ โดยเฉพาะ QA-03 และ QA-07 ต้องได้รับการแก้/ตรวจซ้ำ ส่วน UI/กล้อง, MySQL และรายการรอกติกาต้องทดสอบเพิ่มเติม ช่องหมวด 4 เป็นต้นไปยังคงเดิมทั้งหมด

## วิธีใช้งานและบันทึกผล

- ช่อง `- [ ]` หมายถึงยังไม่ผ่านการยืนยัน เปลี่ยนเป็น `- [x]` เฉพาะเมื่อทำตามกรณีทดสอบและผลจริงตรงกับผลที่คาดหวัง
- ถ้าล้มเหลว ให้คงช่องว่างและบันทึก `FAIL + Bug ID`; ถ้าทดสอบไม่ได้ใช้ `BLOCKED + เหตุผล`; ถ้าไม่เกี่ยวข้องใช้ `N/A + เหตุผล` ห้ามนับเป็นผ่าน
- แต่ละรายการเขียนเป็น **เงื่อนไข/การกระทำ → ผลที่คาดหวัง** หากรายการระบุหลายค่าทดสอบ ต้องผ่านทุกค่าก่อนติ๊ก
- P0 = ต้องทดสอบก่อนเปิดใช้งาน/ก่อนปล่อยแก้ไข, P1 = ฟังก์ชันหลักและเคสขอบเขต, P2 = ประสบการณ์ใช้งานและความเข้ากันได้
- รายการที่ติด `[ยืนยันกติกา]` ต้องตกลง expected result และบันทึกก่อนตัดสินผ่าน ไม่ถือว่าพฤติกรรมปัจจุบันถูกต้องโดยอัตโนมัติ
- ทดสอบการลบข้อมูล, reset, จำลองเวลา, โหลดสูง และ payload ผิดรูปแบบบนฐานข้อมูลทดสอบที่แยกจากผู้ใช้จริงเท่านั้น

| ข้อมูลรอบทดสอบ | ค่า |
| --- | --- |
| ผู้ทดสอบ | ____________________ |
| วันที่/เวลาเริ่ม–จบ | ____________________ |
| Commit / Build | ____________________ |
| Frontend URL / Backend URL | ____________________ |
| Environment / Database | ____________________ |
| Browser / Version / OS / Device | ____________________ |
| Timezone ของ browser / server | ____________________ |
| ชุดข้อมูล / หลักฐาน | ____________________ |

## กติกาอ้างอิงจากโค้ด

| หัวข้อ | พฤติกรรมอ้างอิง |
| --- | --- |
| ผู้ใช้ | นักศึกษา `STUDENT` และผู้ดูแล `ADMIN` |
| สนาม | Court 1–4 |
| วันจอง | วันนี้ตาม `Asia/Bangkok` เท่านั้น |
| เวลา | รอบเริ่ม 08:00–22:00 รวม 15 รอบ รอบสุดท้ายสิ้นสุด 23:00 |
| ระยะเวลา | รอบละ 1 ชั่วโมง; จองกลางรอบได้แต่สิ้นสุดตามรอบเดิม |
| โควตาต่อวัน | มี `PENDING`, `CHECKED_IN` หรือ `COMPLETED` ของวันนั้นแล้ว จองเพิ่มไม่ได้; `CANCELLED` ไม่ใช้โควตา |
| สถานะ | `PENDING` → `CHECKED_IN` → `COMPLETED`; `PENDING` → `CANCELLED` |
| เช็กอินเร็วสุด | ก่อนเริ่มรอบ 15 นาที ตาม backend |
| Check-in deadline | `max(เวลาเริ่มรอบ, เวลาสร้างการจอง) + 15 นาที`; ข้อมูลเก่าไม่มี `created_at` ใช้เวลาเริ่มรอบ |
| เช็กอินช้าสุด | ต้องก่อนทั้ง deadline และเวลาสิ้นสุดรอบ; ณ เวลาเท่ากันถือว่าหมดสิทธิ์ |
| ผิดกฎ | ยกเลิกช้า/ไม่เช็กอินเพิ่ม 1 strike เมื่อ deadline ไม่เกินเวลาสิ้นสุดรอบ |
| รอบสั้น | ถ้ารอบจบก่อน deadline ให้ยกเลิกอัตโนมัติโดยไม่เพิ่ม strike |
| ระงับสิทธิ์ | ครบ 2 strikes แบน 24 ชั่วโมง; เมื่อพ้นแบน reset strikes เป็น 0 |
| Cron | ตรวจรายการทุกนาที; การแสดงสถานะอาจรอรอบงานถัดไป |
| JWT | อายุ 1 วัน; protected API ตอบ 401 เมื่อยืนยันตัวตนไม่ผ่าน |

แหล่งอ้างอิงหลัก: `backend/src/bookings/bookings.service.ts`, `backend/src/bookings/booking-time.ts`, `backend/src/cron/cron.service.ts`, `backend/src/auth/`, `backend/src/users/`, `backend/src/courts/`, `frontend/src/app/`, `frontend/src/components/` และ `frontend/src/lib/api.ts`

## 1. เตรียมระบบและข้อมูลทดสอบ — P0

- [ ] SET-01 เปิด frontend และ backend → เข้าเว็บและเรียก API ผ่าน `/api` ได้ ไม่มีการชี้ไปผิด environment
- [x] SET-02 ตรวจการเชื่อมต่อฐานข้อมูลและ seed → เริ่มระบบได้ ข้อมูล seed ไม่ซ้ำเมื่อ restart
- [x] SET-03 เตรียมนักศึกษา A/B คนละบัญชี ผู้ดูแล และผู้ใช้ไม่ล็อกอิน → ระบุเจ้าของข้อมูลแต่ละชุดได้
- [x] SET-04 เตรียมนักศึกษา strikes 0, 1, 2, แบนยังไม่หมด และแบนหมดแล้ว → มีเวลาหมดแบนที่ตรวจสอบได้
- [x] SET-05 เตรียมการจองครบ 4 สถานะ ทั้งวันนี้/วันก่อน และรายการเก่าไม่มี `created_at` → แต่ละชุดไม่รบกวนกัน
- [x] SET-06 เตรียมรอบว่าง รอบเต็มบางสนาม รอบเต็ม 4 สนาม และวันเต็ม 60 ช่อง → จำนวนในฐานข้อมูลตรงกับ fixture
- [x] SET-07 ตั้งเวลาทดสอบแบบควบคุมได้และบันทึก timezone → ทำซ้ำเคสก่อน/ตรง/หลัง deadline ได้
- [x] SET-08 สำรองข้อมูลทดสอบและทดลองคืนค่า → คืนสภาพเริ่มต้นหลังทดสอบ reset/ลบข้อมูลได้

## 2. Smoke test แบบครบเส้นทาง — P0

- [ ] SMK-01 สมัครนักศึกษาใหม่ → เข้าสู่ Dashboard ด้วยข้อมูลบัญชีใหม่ได้
- [ ] SMK-02 ล็อกอินนักศึกษา → เลือกวันนี้/เวลา/สนาม → ยืนยัน → มีการจอง `PENDING` เพียงรายการเดียว
- [ ] SMK-03 ผู้ดูแลเปิดรายการดังกล่าว → นักศึกษาสแกน QR สนามตรงกันในเวลาที่อนุญาต → ทั้งสองหน้าจอแสดง `CHECKED_IN`
- [x] SMK-04 รอสิ้นสุดรอบและ Cron → รายการเปลี่ยนเป็น `COMPLETED` และประวัติยังอยู่
- [ ] SMK-05 จองแล้วกดยกเลิกทันเวลา → `CANCELLED`, ไม่มี strike และผู้ใช้อื่นจองช่องเดิมได้
- [x] SMK-06 ปล่อยไม่เช็กอินจนพ้น deadline → Cron ยกเลิกและเพิ่ม strike เพียงครั้งเดียว
- [x] SMK-07 ทำผิดกฎครบ 2 ครั้ง → จองไม่ได้ระหว่างแบน และกลับมาจองได้เมื่อครบ 24 ชั่วโมง
- [ ] SMK-08 ล็อกเอาต์แล้วเปิด URL หน้าป้องกันโดยตรง → ต้องล็อกอินใหม่และไม่เห็นข้อมูลส่วนตัวเดิม

## 3. สมัครสมาชิก `/register` — P1

- [ ] REG-01 กรอกข้อมูลครบด้วยอีเมล `@kmitl.ac.th` → สมัครสำเร็จและข้อมูลผู้ใช้ตรงกับที่กรอก
- [ ] REG-02 เว้นแต่ละช่องบังคับทีละช่อง ทั้ง UI และยิง API → แจ้งข้อมูลที่ขาด ไม่สร้างบัญชีไม่สมบูรณ์
- [x] REG-03 อีเมลต่างโดเมน เช่น `@gmail.com` หรือ `@kmitl.ac.th.evil.test` → ปฏิเสธ
- [ ] REG-04 อีเมลผิดรูปแบบ มีแต่ `@kmitl.ac.th` หรือมีช่องว่างแทรก → ปฏิเสธ ไม่เกิด 500
- [x] REG-05 ใช้ username ซ้ำกับนักศึกษาหรือผู้ดูแล → ปฏิเสธพร้อมข้อความเข้าใจได้
- [ ] REG-06 ใช้ student ID หรือ email ซ้ำ → ไม่เขียนทับบัญชีเดิมและไม่เปิดเผยรายละเอียดฐานข้อมูล
- [ ] REG-07 ส่ง student ID ว่าง ยาวเกิน 8 ตัว และผิดรูปแบบที่ตกลง → ปฏิเสธทั้ง frontend/backend
- [ ] REG-08 ส่งชื่อภาษาไทย อังกฤษ ชื่อหลายคำ และช่องว่างหัวท้าย → จัดเก็บ/แสดงชื่อไม่สูญหาย
- [ ] REG-09 ส่งเบอร์โทรยาวเกิน 15 ตัว และข้อความในช่องปีการศึกษา → แจ้ง validation ตามกติกา ไม่บันทึกข้อมูลเสีย
- [ ] REG-10 `[ยืนยันกติกา]` ปีการศึกษา 0, ติดลบ, ทศนิยม และเกินช่วง → ยอมรับเฉพาะช่วงที่กำหนด
- [ ] REG-11 ส่ง password ว่างหรือไม่ส่งผ่าน API → ไม่สร้างบัญชีด้วยรหัสผ่านเริ่มต้นโดยไม่แจ้งผู้ใช้
- [ ] REG-12 `[ยืนยันกติกา]` ทดสอบความยาวรหัสผ่านต่ำสุด/สูงสุดและอักขระที่อนุญาต → UI/API ใช้กติกาเดียวกัน
- [ ] REG-13 `[ยืนยันกติกา]` username/email ตัวพิมพ์ใหญ่–เล็กและช่องว่างหัวท้าย → การตรวจซ้ำและล็อกอินสอดคล้องกัน
- [ ] REG-14 กดสมัครรัว/ส่งคำขอเหมือนกันพร้อมกัน → สร้างได้บัญชีเดียว ไม่มีข้อมูลค้างบางส่วน
- [ ] REG-15 API สมัครล้มเหลว/เครือข่ายหลุด → ยังคงข้อมูลที่จำเป็นให้แก้ไขและลองใหม่ได้ ไม่แสดงว่าสำเร็จ
- [ ] REG-16 สมัครแล้วออกและเข้าใหม่ → รหัสผ่านใช้ได้ และ response ไม่มี password/hash

## 4. ล็อกอินและ Session — P0

- [ ] AUT-01 username/password นักศึกษาถูกต้อง → ได้สิทธิ์ STUDENT และ Dashboard นักศึกษา
- [ ] AUT-02 username/password ผู้ดูแลถูกต้อง → ได้สิทธิ์ ADMIN และ Dashboard ผู้ดูแล
- [ ] AUT-03 username ไม่มีจริงหรือ password ผิด → 401 และข้อความ login ผิดพลาด ไม่ redirect วน
- [ ] AUT-04 ส่ง username/password ว่าง, null, array หรือ object ผ่าน API → ปฏิเสธแบบควบคุมได้ ไม่เกิด 500
- [ ] AUT-05 กดล็อกอินซ้ำขณะโหลด → ไม่มี submit ซ้ำหรือ session สลับผิดบัญชี
- [ ] AUT-06 รีเฟรชหลังล็อกอินด้วย token ใช้ได้ → ยังคงบัญชีและสิทธิ์ถูกต้อง
- [ ] AUT-07 เปิด protected API โดยไม่มี token → 401 และไม่มีข้อมูลส่วนตัวกลับมา
- [ ] AUT-08 ใช้ token หมดอายุ ลายเซ็นผิด ถูกแก้ payload หรือผิดรูปแบบ → API ปฏิเสธ ไม่เชื่อ role ที่ปลอม
- [ ] AUT-09 token ปัจจุบันโดน 401 จาก protected API → ลบ token/user เดิม กลับ `/login` และไม่ขึ้น Axios console error ซ้ำจากส่วนที่จัดการแล้ว
- [ ] AUT-10 หลาย request ตอบ 401 พร้อมกัน → redirect ครั้งเดียว ไม่มี loop หรือคำขอไม่สิ้นสุด
- [ ] AUT-11 request เก่าตอบ 401 หลังล็อกอินด้วย token ใหม่ → ไม่ลบ session ใหม่
- [ ] AUT-12 login/register ตอบ 401 → form ยังรับ error มาแสดงได้ ไม่ถูกจัดเป็น session หมดอายุ
- [ ] AUT-13 API ตอบ 403/500 หรือ network error → ไม่ล้าง session ที่ยังใช้ได้
- [ ] AUT-14 token ถูกต้องแต่บัญชีถูกลบ → API ปฏิเสธและ frontend ออกจาก session เดิมอย่างเหมาะสม
- [ ] AUT-15 กด logout → token/user ถูกลบ และ Back/Refresh ไม่คืนสิทธิ์เข้าข้อมูล
- [ ] AUT-16 logout ในอีกแท็บ → แท็บเดิมตรวจพบการเปลี่ยน session และไม่ทำงานต่อด้วยบัญชีเก่า
- [ ] AUT-17 localStorage ไม่มี user, มี JSON เสีย หรือ token เป็น `null`/`undefined` แบบข้อความ → เว็บไม่ crash และนำกลับเข้าสู่ระบบได้
- [ ] AUT-18 browser ไม่อนุญาต localStorage → มีพฤติกรรม fallback/แจ้งปัญหาชัดเจน ไม่มี loading ค้าง
- [ ] AUT-19 เปลี่ยนบัญชี A เป็น B ใน browser เดิม → รายการจองและ notifications ของ A ไม่ค้างให้ B เห็น

## 5. สิทธิ์เข้าถึงและแยกข้อมูลผู้ใช้ — P0

- [ ] ACL-01 Guest เปิด `/dashboard`, `/booking`, `/booking/select-court`, `/scan`, `/admin/users` → ต้องล็อกอิน
- [ ] ACL-02 STUDENT เปิด `/admin/users` โดยตรง → ไม่เห็นหน้าจอ/ข้อมูลผู้ดูแล
- [ ] ACL-03 STUDENT ยิง API ดูผู้ใช้ทั้งหมด/เพิ่ม admin/ลบผู้ใช้ → 403 และข้อมูลไม่เปลี่ยน
- [ ] ACL-04 STUDENT ยิง API ดูการจองทั้งหมด/admin notifications/finish/reset → 403
- [ ] ACL-05 A เรียก `/bookings/me` และ `/bookings/user-notifications` → ได้เฉพาะของ A
- [ ] ACL-06 A เปลี่ยน booking ID เป็นของ B เพื่อเช็กอิน/ยกเลิก → ปฏิเสธและ B ไม่ได้รับผลกระทบ
- [ ] ACL-07 แก้ role/id/user ใน localStorage หรือส่ง stu_id ของ B ใน body → backend ยังยึดตัวตนจาก token
- [ ] ACL-08 ADMIN เปิดหน้าจองและสแกน → กลับ Dashboard ตาม flow ปัจจุบัน; ยิง API ตรงต้องไม่สร้างการจองนักศึกษาผิดตัว
- [ ] ACL-09 เรียก availability แบบ Guest → อ่านสถานะสนามได้ตาม API ปัจจุบัน แต่ไม่เปิดเผยชื่อ เบอร์โทร email หรือ token
- [ ] ACL-10 ทดสอบทุก protected endpoint ด้วย token หาย/เสีย → ไม่มี endpoint หลุดการตรวจสิทธิ์

## 6. เลือกวัน เวลา และสนาม — P1

- [ ] AVA-01 เปิด `/booking` → แสดงกติกา ปิดได้ และมีวันนี้ตามเวลาไทย
- [ ] AVA-02 ตรวจรอบเวลา → มี 15 รอบ ตั้งแต่ 08:00–09:00 ถึง 22:00–23:00 ไม่มีซ้ำ/ขาด
- [ ] AVA-03 ยังไม่เลือกเวลาแล้วกดถัดไป → แจ้งให้เลือกและไม่ไปจองด้วยค่าว่าง
- [ ] AVA-04 เลือกรอบว่าง → จำนวนสนามว่างตรงกับ Court 1–4 และข้อมูล API
- [ ] AVA-05 บางสนามมี PENDING/CHECKED_IN → สนามนั้นเลือกไม่ได้และยอดว่างถูกต้อง
- [ ] AVA-06 รอบที่ทั้ง 4 สนามเต็ม → แสดงเต็มและไม่เปิดให้ยืนยันจอง
- [ ] AVA-07 มี CANCELLED → ช่องนั้นกลับมาว่าง; การจองคนละวันไม่กระทบวันนี้
- [ ] AVA-08 มี COMPLETED ก่อนจบรอบ → UI ต้องสอดคล้องกับ backend ที่ยังกันช่องนั้น ไม่แสดงว่าว่างแล้วจองไม่ได้
- [ ] AVA-09 เวลาอยู่กลางรอบ เช่น 10:30 → รอบ 10:00–11:00 ยังเลือกได้ถ้าว่าง
- [ ] AVA-10 ณ 11:00:00 → รอบ 10:00–11:00 เลือกไม่ได้; รอบถัดไปไม่ถูกปิดผิด
- [ ] AVA-11 หลัง 23:00 → ไม่มีรอบวันนี้ที่จองได้และข้อความชัดเจน
- [ ] AVA-12 เปิด select-court โดยไม่มี date/time → กลับหน้าเลือกเวลา ไม่ crash
- [ ] AVA-13 แก้ query เป็นอดีต/พรุ่งนี้/วันที่ไม่มีจริง/เวลาไม่ใช่ต้นชั่วโมง → ปฏิเสธ ไม่สร้างรายการผิด
- [ ] AVA-14 เปิดหน้าไว้จนข้ามวันไทยแล้วกดยืนยัน → ไม่จองวันเก่า และมีทางเลือกโหลดวันใหม่
- [ ] AVA-15 browser ใช้ timezone ต่างจาก server → วันและช่วงเวลาไทยในทุกหน้าตรงกัน
- [ ] AVA-16 availability โหลดช้า/ล้มเหลว/ได้รายการว่างผิดปกติ → แสดง loading/error/empty อย่างถูกต้อง ไม่แสดงสนามว่างหลอก
- [ ] AVA-17 เปิด–ย้อนหน้า/เปลี่ยน query ระหว่าง request → response เก่าไม่ทับข้อมูลวันที่หรือรอบล่าสุด

## 7. ยืนยันและสร้างการจอง — P0

- [ ] BKG-01 เลือกสนาม → dialog แสดงชื่อผู้จอง วัน เดือน สนาม และเวลาเริ่ม–สิ้นสุดตรงกัน
- [ ] BKG-02 ปิด dialog/กดยกเลิก → ไม่เกิด POST และไม่มีรายการใหม่
- [ ] BKG-03 ยืนยันสนามว่าง → สร้าง PENDING ถูกเจ้าของ ถูกวัน/สนาม/เวลา และกลับ Dashboard
- [ ] BKG-04 ยืนยันกลางรอบ → time_out ยังเป็นเวลาจบรอบเดิม ไม่เพิ่มอีก 1 ชั่วโมงจากเวลาที่จอง
- [ ] BKG-05 มี PENDING ของวันนี้แล้ว → ปฏิเสธการจองเพิ่มแม้คนละสนาม/รอบ
- [ ] BKG-06 มี CHECKED_IN หรือ COMPLETED ของวันนี้แล้ว → ปฏิเสธการจองเพิ่มตามโควตาปัจจุบัน
- [ ] BKG-07 มีเพียง CANCELLED ของวันนี้และไม่ถูกแบน → จองใหม่ได้
- [ ] BKG-08 มีการจองวันก่อน → ไม่กินโควตาวันนี้
- [ ] BKG-09 บัญชีอยู่ระหว่างแบน → UI แจ้งและ backend ปฏิเสธการจอง
- [ ] BKG-10 ยืนยันขณะที่ผู้อื่นจองสนาม/รอบนั้นไปแล้ว → แจ้งชนและ refresh availability ไม่สร้างซ้ำ
- [ ] BKG-11 กดปุ่มยืนยันรัว/กด Enter ซ้ำ → ได้รายการเดียวและปุ่มแสดงสถานะโหลด
- [ ] BKG-12 ส่ง courtId 0, -1, 5, ทศนิยม, string ผิดรูป, null → ปฏิเสธ ไม่สร้างสนามที่ไม่มีจริง
- [ ] BKG-13 ส่ง startTime `07:00`, `23:00`, `24:00`, `10:30`, `ab:cd` → ปฏิเสธ
- [ ] BKG-14 ส่ง `08:00` และ `08:00:00` ใน fixture แยก → ได้รอบเดียวกันและตรวจการชนได้เหมือนกัน
- [ ] BKG-15 ไม่ส่ง date/courtId/startTime หรือส่งชนิดข้อมูลผิด → controlled 4xx ไม่ใช่ 500
- [ ] BKG-16 request รอคิวจนรอบสิ้นสุดก่อน transaction เริ่ม → ปฏิเสธ ไม่สร้างรายการหมดเวลาแล้ว
- [ ] BKG-17 request สำเร็จที่ server แต่ client timeout แล้วลองใหม่ → ไม่มี duplicate และค้นรายการที่สำเร็จไปแล้วได้
- [ ] BKG-18 transaction ล้มเหลวระหว่างบันทึก → ไม่มีรายการบางส่วน ไม่ล็อกฐานข้อมูลค้าง และคำขอถัดไปทำงานได้

## 8. Dashboard นักศึกษา — P1

- [ ] DSH-01 ไม่มีการจอง → แสดง empty state และทางไปจองสนาม
- [ ] DSH-02 มีการจอง → ชื่อสนาม วัน เวลา สถานะ และ booking ID ตรงกับฐานข้อมูล
- [ ] DSH-03 มีประวัติหลายวัน → เรียงวัน/เวลาจากใหม่ไปเก่าถูกต้อง
- [ ] DSH-04 การจองทั้ง 4 สถานะ → badge, สี และปุ่มที่อนุญาตตรงสถานะ
- [ ] DSH-05 countdown เดินถึงเวลาจบรอบ → ไม่ติดลบ ไม่แสดง NaN และจองกลางรอบไม่ขยายเวลา
- [ ] DSH-06 refresh หลังจอง/ยกเลิก/เช็กอิน/จบรอบ → สถานะตรงกับ backend
- [ ] DSH-07 ชื่อยาว ภาษาไทย หรือข้อมูลบางช่องหาย → layout ไม่พังและไม่แสดง `undefined`
- [ ] DSH-08 โหลดข้อมูลล้มเหลว → ไม่แสดงว่าผู้ใช้ไม่มีการจองโดยไม่บอกข้อผิดพลาด

## 9. QR และเช็กอิน `/scan` — P0

- [ ] QR-01 อนุญาตกล้องบน HTTPS/localhost → scanner เปิดและอ่าน QR หมายเลขสนามได้
- [ ] QR-02 ปฏิเสธสิทธิ์กล้อง/ไม่มีกล้อง/กล้องถูกแอปอื่นใช้ → แจ้งวิธีแก้และออกจากหน้าได้
- [ ] QR-03 เปิดผ่าน HTTP บนอุปกรณ์จริง → แจ้งข้อจำกัดการใช้กล้อง ไม่ค้างเงียบ
- [ ] QR-04 QR สนามตรงกับ PENDING ของผู้ใช้และอยู่ในเวลา → เปลี่ยน CHECKED_IN เพียงครั้งเดียว
- [ ] QR-05 QR คนละสนาม/ไม่มี PENDING ของตน → ปฏิเสธ ไม่เช็กอินรายการคนอื่น
- [ ] QR-06 QR ว่าง ข้อความ URL หรือเลขนอก 1–4 → ปฏิเสธและสามารถเริ่มสแกนใหม่ได้
- [ ] QR-07 QR `1abc`, `1.5`, `-1` → ไม่ถูกตีความเป็น Court 1 โดยการตัดข้อความบางส่วน
- [ ] QR-08 อ่าน QR เดิมหลายเฟรม/แตะซ้ำเร็ว → ไม่ส่งเช็กอินซ้ำและไม่เกิด toast ซ้อน
- [ ] QR-09 เปิด scan ก่อนข้อมูล bookings โหลดเสร็จ → ไม่แจ้งผิดว่าไม่มีการจองถาวร และลองใหม่ได้
- [ ] QR-10 ออกจากหน้าหรือ Back ระหว่างสแกน → กล้องหยุด ไม่ค้างไฟกล้อง/DOM scanner ซ้ำ
- [ ] QR-11 request เช็กอินล้มเหลว/เครือข่ายหลุด → ไม่แสดงเช็กอินสำเร็จ และกลับมาลองใหม่ได้
- [ ] QR-12 ทดสอบกล้องหลังมือถือ แนวตั้ง/แนวนอน แสงน้อย และ QR ย่อขนาด → อ่านได้หรือแจ้งสถานะชัดเจน
- [ ] QR-13 `[ยืนยันกติกา]` เปิด QR ก่อนรอบ 15 นาที → UI ผู้ดูแลและสิทธิ์ backend ต้องสอดคล้องกัน (ปัจจุบัน UI รอถึงเวลาเริ่มรอบ)
- [ ] QR-14 ส่ง check-in โดยตรงด้วย booking ID ไม่พบ/ผิดรูป/ของคนอื่น → controlled 4xx ไม่มีข้อมูลรั่ว

## 10. ขอบเขตเวลาเช็กอิน — P0

ใช้รอบ 10:00–11:00 ตามเวลาไทย และแยก fixture สำหรับแต่ละกรณี ห้ามใช้รายการที่เช็กอินแล้วซ้ำ

- [ ] TIM-01 จองก่อนรอบ; เช็กอิน 09:44:59 → ปฏิเสธว่าเร็วเกินไป
- [ ] TIM-02 จองก่อนรอบ; เช็กอิน 09:45:00 → backend ยอมรับ
- [ ] TIM-03 จองก่อนรอบ; เช็กอิน 10:14:59 → ยอมรับ
- [ ] TIM-04 จองก่อนรอบ; เช็กอิน 10:15:00 และ 10:15:01 → ปฏิเสธแม้ Cron ยังไม่เปลี่ยนสถานะ
- [ ] TIM-05 จองเวลา 10:30; เช็กอิน 10:44:59 → ยอมรับตาม deadline 10:45
- [ ] TIM-06 จองเวลา 10:30; เช็กอิน 10:45:00 → ปฏิเสธ
- [ ] TIM-07 จองเวลา 10:50; เช็กอิน 10:59:59 → ยอมรับแม้ deadline จะเป็น 11:05
- [ ] TIM-08 จองเวลา 10:50; เช็กอิน 11:00:00 → ปฏิเสธเพราะรอบจบก่อน deadline
- [ ] TIM-09 ข้อมูลเก่าไม่มี created_at → ใช้ deadline 10:15 ไม่เกิด invalid date
- [ ] TIM-10 เปลี่ยนเวลาบนเครื่อง client ให้ช้าหรือเร็ว → ไม่หลบข้อจำกัดเวลาที่ backend บังคับได้
- [ ] TIM-11 ทดสอบวันนี้ช่วง 00:00–06:59 ไทย เมื่อวัน UTC ยังเป็นเมื่อวาน → API/หน้าจอไม่เลือกวันผิด โดยเฉพาะ availability ที่ไม่ส่ง date

## 11. ยกเลิกการจอง — P0

- [ ] CAN-01 เปิด dialog ยกเลิกแล้วกดไม่ยืนยัน → PENDING คงเดิม
- [ ] CAN-02 ยืนยันยกเลิกก่อน deadline → CANCELLED และ strikes ไม่เพิ่ม
- [ ] CAN-03 ยกเลิกตรง deadline เมื่อ deadline ≤ เวลาจบรอบ → เพิ่ม 1 strike
- [ ] CAN-04 ยกเลิกหลัง deadline แต่ก่อน Cron จัดการ → เพิ่ม 1 strike และ CANCELLED
- [ ] CAN-05 ยกเลิกรอบสั้นที่ deadline > เวลาจบรอบ → ไม่เพิ่ม strike
- [ ] CAN-06 ยกเลิก CHECKED_IN/COMPLETED/CANCELLED → ปฏิเสธ ไม่เปลี่ยนสถานะหรือ strike
- [ ] CAN-07 ยกเลิกรายการเดียวกันพร้อมกันสองคำขอ → สำเร็จครั้งเดียว strike ไม่เพิ่มซ้ำ
- [ ] CAN-08 ยกเลิกสำเร็จ → รายการ/availability/notifications สะท้อนสถานะใหม่ และจองใหม่ได้ถ้ายังมีสิทธิ์
- [ ] CAN-09 ยกเลิกล้มเหลว → dialog/loading คืนสภาพและไม่แสดงสำเร็จผิด

## 12. Strikes และการแบน — P0

- [ ] BAN-01 ผู้ใช้ 0 strike ผิดกฎครั้งแรก → เป็น 1 strike ยังไม่แบน
- [ ] BAN-02 ผู้ใช้ 1 strike ผิดกฎอีกครั้ง → เป็น 2 และ banned_until อีก 24 ชั่วโมงจากเหตุการณ์
- [ ] BAN-03 ระหว่างแบนเปิดเว็บ → popup แสดงจำนวน strike/เวลาหมดแบนถูกต้อง
- [ ] BAN-04 ปิด popup แล้วพยายามจองผ่าน API → backend ยังปฏิเสธ
- [ ] BAN-05 ก่อนหมดแบน 1 วินาที → ยังจองไม่ได้
- [ ] BAN-06 ณ เวลาหมดแบน → ตรวจ ban-status/สร้าง booking/Cron ปลดแบนตามแต่ละเส้นทางและ reset strikes เป็น 0
- [ ] BAN-07 ADMIN เรียก ban-status → ไม่ถูกแสดง popup แบนแบบนักศึกษา
- [ ] BAN-08 logout ผู้ใช้ที่ถูกแบนแล้วเข้าอีกบัญชี → popup/ข้อมูลแบนไม่ติดข้ามบัญชี
- [ ] BAN-09 เรียก ban-status ซ้ำพร้อม Cron → ไม่ย้อนค่าการแบนหรือทำ strikes ผิดจากเหตุการณ์ล่าสุด

## 13. งานอัตโนมัติ Cron — P0

- [ ] CRN-01 PENDING ก่อน cutoff → ยังไม่เปลี่ยนสถานะ
- [ ] CRN-02 PENDING ถึง `min(deadline, end)` → CANCELLED ในรอบ Cron ถัดไป
- [ ] CRN-03 deadline ≤ end และไม่เช็กอิน → เพิ่ม strike 1 ครั้งเท่านั้น
- [ ] CRN-04 deadline > end → CANCELLED โดยไม่เพิ่ม strike
- [ ] CRN-05 deadline เท่ากับ end พอดี → ยกเลิกพร้อม strike ตามเงื่อนไขปัจจุบัน
- [ ] CRN-06 CHECKED_IN ก่อน end → ยัง CHECKED_IN; ณ/หลัง end → COMPLETED
- [ ] CRN-07 รัน Cron ซ้ำกับชุดเดิม → ไม่เพิ่ม strike ซ้ำหรือเปลี่ยนสถานะย้อนหลัง
- [ ] CRN-08 มีหลายรายการของผู้ใช้เดียวกันในข้อมูลเก่า → รวม strikes ไม่สูญหายจากการเขียนทับ
- [ ] CRN-09 หยุด backend ข้าม cutoff แล้วเปิดใหม่ → Cron รอบถัดไปจัดการรายการตกค้างได้
- [ ] CRN-10 Cron บันทึกล้มเหลวกลาง transaction → ไม่มี booking กับ strikes ที่ไม่สัมพันธ์กัน และรอบถัดไปกู้การทำงานได้
- [ ] CRN-11 ข้ามเที่ยงคืน → วันใหม่จองได้ตามโควตาใหม่ แต่ประวัติวันก่อนยังอยู่ ไม่ถือว่ามีการลบประวัติรายวัน

## 14. Dashboard ผู้ดูแลและจบการใช้งาน — P1

- [ ] ADM-01 เปิดด้วย ADMIN → เห็นรายการของนักศึกษาทั้งหมด พร้อมชื่อและรายละเอียดตรงกับข้อมูลจริง
- [ ] ADM-02 เปิดรายการ → dialog แสดงสนาม วัน เวลา เจ้าของ และสถานะถูกต้อง
- [ ] ADM-03 ข้อมูลเปลี่ยนจากอีกอุปกรณ์ → polling ประมาณทุก 5 วินาทีอัปเดตรายการได้
- [ ] ADM-04 รายการที่เปิด dialog อยู่เปลี่ยนสถานะ → dialog ใช้ข้อมูลล่าสุด ไม่ค้าง QR/ปุ่มเก่า
- [ ] ADM-05 กด Finish early ของ CHECKED_IN แล้วยกเลิก confirm → สถานะไม่เปลี่ยน
- [ ] ADM-06 ยืนยัน Finish early → COMPLETED และแสดงผลทั้งผู้ดูแล/นักศึกษา
- [ ] ADM-07 finish รายการ PENDING/CANCELLED/COMPLETED หรือ ID ไม่มีจริง → ปฏิเสธอย่างเหมาะสม
- [ ] ADM-08 กด finish ซ้ำ/ผู้ดูแลสองคนกดพร้อมกัน → ผลสุดท้ายสอดคล้อง ไม่มีการคืนสถานะเก่า
- [ ] ADM-09 finish ก่อนจบรอบ → โควตานักศึกษาและช่องสนามยังเป็นไปตามกติกา COMPLETED
- [ ] ADM-10 สลับช่วงสถิติ Day/Week/Month/Year → ตัวเลข/กราฟสัมพันธ์กับข้อมูล fixture ไม่ใช่แค่ปุ่มเปลี่ยนสี
- [ ] ADM-11 ไม่มีข้อมูลหรือมีจำนวนมาก → สถิติไม่ NaN ตารางไม่ล้น และเข้าถึงรายการได้
- [ ] ADM-12 ออกจาก Dashboard → polling/timer หยุด ไม่สร้าง request ซ้อนทุกครั้งที่กลับมา

## 15. จัดการผู้ใช้และ Reset — P0 สำหรับสิทธิ์/ข้อมูล, P1 สำหรับ UI

- [ ] USR-01 ADMIN เปิด `/admin/users` → แสดงนักศึกษาครบและข้อมูลถูกช่อง
- [ ] USR-02 สร้าง admin ด้วยข้อมูลถูกต้อง → ล็อกอินบัญชีใหม่และใช้สิทธิ์ ADMIN ได้
- [ ] USR-03 สร้าง admin ด้วย username/admin ID ซ้ำหรือช่องจำเป็นว่าง → ปฏิเสธ ไม่เขียนทับบัญชีเดิม
- [ ] USR-04 สร้าง admin ผ่าน API โดยไม่ส่ง password → ไม่ได้รับบัญชีรหัสผ่านเริ่มต้นโดยไม่ตั้งใจ
- [ ] USR-05 กดลบนักศึกษาแล้ว cancel confirm → ผู้ใช้ยังอยู่
- [ ] USR-06 ยืนยันลบนักศึกษาที่ไม่มี booking → หายจากรายการและล็อกอินไม่ได้
- [ ] USR-07 ลบนักศึกษาที่มีประวัติ booking → ตาม FK RESTRICT ต้องไม่ลบประวัติโดยเงียบ และ UI แจ้งสาเหตุเข้าใจได้
- [ ] USR-08 ลบ ID ไม่พบ/ลบซ้ำ → ผลลัพธ์สม่ำเสมอ ไม่กระทบผู้ใช้อื่น
- [ ] USR-09 เพิ่มหรือลบล้มเหลว → ไม่มี toast สำเร็จผิดและลองใหม่ได้
- [ ] RST-01 ADMIN กด reset แล้วปฏิเสธการยืนยันขั้นแรกหรือขั้นที่สอง → ไม่มีข้อมูลถูกลบ
- [ ] RST-02 ยืนยัน reset ทั้งสองขั้นในฐานทดสอบ → ลบ bookings ทั้งหมดตามขอบเขต แต่บัญชี สนาม strikes และ ban ไม่ถูกลบตามไป
- [ ] RST-03 หลัง reset → Dashboard, notifications และ availability ไม่มีข้อมูลการจองเดิมค้าง
- [ ] RST-04 reset ล้มเหลวหรือถูกเรียกพร้อม booking/check-in → ไม่เกิดข้อมูลกำพร้า และผลสำเร็จที่แจ้งตรงกับข้อมูลสุดท้าย

## 16. Notifications และข่าว — P1/P2

- [ ] NTF-01 นักศึกษาเปิดกระดิ่ง → แสดงเฉพาะของตน เรียงล่าสุดก่อน สูงสุด 20 booking notifications
- [ ] NTF-02 ผู้ดูแลเปิดกระดิ่ง → แสดง PENDING/CANCELLED ล่าสุดไม่เกิน 20 รายการตาม API ปัจจุบัน
- [ ] NTF-03 สถานะทั้ง 4 แบบ → ข้อความแจ้งเตือนถูกต้อง ไม่ใช้ข้อความจองสำเร็จกับสถานะยกเลิก
- [ ] NTF-04 วันนี้ครบ 60 ช่องที่ถูกใช้ตามสถานะอ้างอิง → นักศึกษาเห็นข้อความสนามเต็ม; รายการระบบไม่แสดงสนาม/เวลาเป็น undefined
- [ ] NTF-05 มีเพียง 59 ช่องหรือรายการเต็มเป็นคนละวัน → ไม่แสดงข้อความเต็มวันนี้
- [ ] NTF-06 ไม่มี notifications → แสดง empty state ที่อ่านเข้าใจได้
- [ ] NTF-07 `[ยืนยันกติกา]` เปิดอ่านแล้วปิด/รีเฟรช → badge อ่านแล้วเป็นไปตามข้อกำหนด; ปัจจุบันยังไม่มี API บันทึก read state
- [ ] NTF-08 `[ยืนยันกติกา]` มีเหตุการณ์ใหม่ขณะเปิดหน้า → ความถี่อัปเดต notifications ตรงกับที่ตกลง ไม่อ้างว่า realtime โดยยังไม่ทดสอบ
- [ ] NWS-01 เปิด `/news` และส่วนข่าวหน้าแรก → ข้อความ/รูป/วันที่อ่านได้และ navigation ใช้งานได้
- [ ] NWS-02 การ์ดข่าว/ลิงก์ที่ดูเหมือนกดได้ → ไปปลายทางจริง หรือออกแบบให้ชัดว่าเป็นเนื้อหา static
- [ ] NWS-03 `[ยืนยันกติกา]` Guest เปิด `/news` → พฤติกรรม public/ต้องล็อกอินตรงกับนโยบายที่ตกลง

## 17. API contract และ validation — P1

Frontend เรียกเส้นทางด้านล่างผ่าน prefix `/api`; เมื่อต่อ backend โดยตรงไม่ใช้ prefix นี้

| Endpoint | ผู้เรียกตาม flow ปัจจุบัน | ตรวจหลัก |
| --- | --- | --- |
| POST /auth/register | Guest | สมัคร, validation, duplicate |
| POST /auth/login | Guest | credentials, token, role |
| GET /courts/availability?date= | Public | วัน, 4 สนาม, สถานะ, ไม่มีข้อมูลส่วนตัว |
| POST /bookings | นักศึกษา | โควตา, เวลา, สนาม, ban, concurrency |
| GET /bookings/me | ผู้ล็อกอิน | เฉพาะเจ้าของ |
| GET /bookings?date= | ADMIN | ทั้งหมด/กรองวัน |
| GET /bookings/notifications | ADMIN | สถานะและจำนวน |
| GET /bookings/user-notifications | ผู้ล็อกอิน | เฉพาะเจ้าของและข้อความเต็ม |
| POST /bookings/:id/check-in | เจ้าของรายการ | สนาม, เวลา, สถานะ |
| POST /bookings/:id/cancel | เจ้าของรายการ | สถานะและ strike |
| POST /bookings/:id/finish | ADMIN | จบเฉพาะ CHECKED_IN |
| POST /bookings/reset | ADMIN | ลบ bookings |
| GET /users | ADMIN | รายการนักศึกษา |
| POST /users/admin | ADMIN | สร้างผู้ดูแล |
| DELETE /users/:id | ADMIN | ลบและ FK |
| GET /users/me/ban-status | ผู้ล็อกอิน | สถานะตนเอง/ปลดแบน |

- [ ] API-01 ทดสอบ happy path ทุก endpoint ในตาราง → HTTP status, body และผลใน DB ตรงกับงานที่ทำ
- [ ] API-02 แต่ละ endpoint รับ method ผิด/route ไม่มีจริง → controlled 4xx ไม่เกิด mutation
- [ ] API-03 ทดสอบ body หาย, JSON เสีย, type ผิด และ field ยาวมาก → ปฏิเสธโดยไม่ crash หรือเผย stack trace
- [ ] API-04 ส่ง field ส่วนเกิน เช่น role, strikes, banned_until, status, admin_id ในการสมัคร/จอง → ไม่ยกระดับสิทธิ์หรือแก้ค่าที่ server เป็นเจ้าของ
- [ ] API-05 GET bookings ของ ADMIN แบบมี/ไม่มี date → กรองวันจริงและเรียงลำดับถูกต้อง
- [ ] API-06 ตัวระบุ `id` กับ `booking_id` และชื่อเวลาใน response → frontend อ่านได้ตรงกัน ไม่มีแถว key ซ้ำ
- [ ] API-07 error validation, ไม่พบข้อมูล และไม่มีสิทธิ์ → ข้อความมีประโยชน์ ไม่เปิดเผย SQL/รหัสผ่าน/รายละเอียดภายใน
- [ ] API-08 ทดสอบ CORS จาก origin ที่ใช้งานจริง → frontend เรียกได้ตาม deployment และนโยบาย origin ที่ตกลง

## 18. คำขอพร้อมกันและความถูกต้องของข้อมูล — P0

- [ ] RACE-01 นักศึกษา A/B จองสนามและรอบเดียวกันพร้อมกัน → สำเร็จเพียงคนเดียว มี 1 booking ที่ใช้ช่องนั้น
- [ ] RACE-02 นักศึกษาคนเดียวจองคนละสนามพร้อมกัน → สำเร็จเพียงรายการเดียวตามโควตา
- [ ] RACE-03 นักศึกษาคนละคนจองคนละสนามพร้อมกัน → สำเร็จทั้งคู่ถ้าผ่านกติกา ไม่ล็อกกันถาวร
- [ ] RACE-04 check-in แข่งกับ cancel รายการเดียวกัน → มีผลสุดท้ายที่ถูกต้องเพียงเส้นทางเดียว
- [ ] RACE-05 check-in แข่งกับ Cron ตรง deadline → ไม่เช็กอินรายการที่หมดเวลาและไม่เกิดสถานะย้อนกลับ
- [ ] RACE-06 cancel แข่งกับ Cron → CANCELLED และเพิ่ม strike ไม่เกิน 1 ครั้ง
- [ ] RACE-07 Finish early แข่งกับ Cron → COMPLETED สม่ำเสมอ ไม่มีข้อมูลสูญหาย
- [ ] RACE-08 แบนเกิดพร้อมคำขอจองใหม่ → ไม่หลุดให้บัญชีที่ถูกแบนจองเพราะตรวจสถานะก่อนเข้าคิว
- [ ] RACE-09 ถ้า deployment มี backend หลาย process/replica ให้ทำ RACE-01/02/04/06 ซ้ำข้าม instance → DB ยังรักษากติกา ไม่อาศัย mutex ใน process เดียว
- [ ] DATA-01 restart backend/ฐานข้อมูล → บัญชี การจอง และเวลาแบนคงอยู่ตาม storage ที่ตั้งค่า
- [ ] DATA-02 ทดลอง rollback และ restore backup → ความสัมพันธ์ student/admin/booking ไม่เสีย
- [ ] DATA-03 ตรวจ created_at หลัง check-in/cancel/finish → เวลาเริ่มนับ grace period ไม่ถูกเปลี่ยนตาม updated_at
- [ ] DATA-04 ถ้ารองรับทั้ง MySQL และ SQLite ให้ทดสอบชุดหลักทั้งสอง → เวลา transaction ข้อจำกัด unique และ FK ให้ผลเทียบเท่ากัน

## 19. ความปลอดภัยและข้อมูลส่วนตัว — P0/P1

รายการนี้เป็น checklist เพื่อทดสอบ ไม่ใช่ผลการตรวจพบช่องโหว่ที่ยืนยันแล้ว

- [ ] SEC-01 ตรวจ login/register/users/bookings/notifications responses รวม nested student/admin → ไม่มี password หรือ password hash
- [ ] SEC-02 ตรวจฐานข้อมูล → เก็บรหัสผ่านเป็น hash และล็อกอินด้วยรหัสผ่านจริงได้
- [ ] SEC-03 ใส่ HTML/script ในชื่อ/username และเปิดหน้าเกี่ยวข้อง → แสดงเป็นข้อความ ไม่ execute
- [ ] SEC-04 ส่ง SQL-like input ผ่าน login, ID และ query → ไม่ข้ามสิทธิ์ ไม่อ่าน/แก้ข้อมูลเกินขอบเขต
- [ ] SEC-05 แก้ JWT role/sub/username หรือใช้ลายเซ็นผิด → ปฏิเสธทุก protected API
- [ ] SEC-06 ตรวจ production config → ไม่มีบัญชี seed รหัสผ่านง่ายที่ใช้เข้าได้ และ JWT secret ไม่ใช้ค่า hard-coded สำหรับพัฒนา
- [ ] SEC-07 ส่ง login ผิดซ้ำตามขอบเขตที่ตกลง → มีการจำกัดความพยายาม/มาตรการตามนโยบาย ไม่ทำให้ระบบหยุดบริการ
- [ ] SEC-08 ตรวจ URL, console, logs และ error page → ไม่มี token/password หรือข้อมูลส่วนตัวเกินจำเป็น
- [ ] SEC-09 production ใช้ HTTPS → ไม่มี mixed content และข้อมูลล็อกอินไม่ส่งผ่าน HTTP
- [ ] SEC-10 `[ยืนยันกติกา]` ใช้ QR ที่ถ่ายภาพไว้/สร้างเลขสนามเองจากนอกสถานที่ → ประเมินว่าระบบต้องพิสูจน์การอยู่หน้างานหรือยอมรับ QR คงที่
- [ ] SEC-11 `[ยืนยันกติกา]` logout แล้วนำ token เดิมเรียก API → ผลตรงกับนโยบายเพิกถอน token ที่กำหนด; การลบ localStorage อย่างเดียวไม่ใช่ server revocation

## 20. UI, Navigation และ Accessibility — P2

- [ ] UI-01 เปิดทุกหน้า `/`, `/login`, `/register`, `/dashboard`, `/booking`, `/booking/select-court`, `/scan`, `/admin/users`, `/news` ตามสิทธิ์ → ไม่มีหน้าขาว/404 ที่ไม่ตั้งใจ
- [ ] UI-02 โลโก้ เมนู sidebar เมนูมือถือ และปุ่มกลับ → นำทางถูกหน้าและเมนู active ถูกต้อง
- [ ] UI-03 Light/Dark mode ทุกหน้ารวม dialog/toast/QR → ข้อความอ่านได้ สีสถานะไม่กลืนพื้นหลัง
- [ ] UI-04 เปลี่ยนธีมแล้ว refresh/เปลี่ยนหน้า → ธีมคงอยู่ ไม่มี hydration error
- [ ] UI-05 หน้าจอ 320, 375, 768, 1024, 1440 px → ไม่มีเนื้อหา/ปุ่มสำคัญล้นหรือถูกบัง
- [ ] UI-06 มือถือแนวนอนและ zoom 200% → ใช้ flow สมัคร/จอง/ยกเลิก/เช็กอินได้
- [ ] UI-07 ใช้คีย์บอร์ด Tab/Shift+Tab/Enter/Escape → เข้าถึงปุ่ม ฟอร์ม เมนู และปิด dialog ได้
- [ ] UI-08 เปิด modal → focus อยู่ใน modal; ปิดแล้วกลับปุ่มเดิม และหน้าหลังไม่ถูกกดทะลุ
- [ ] UI-09 input มี label, ปุ่มไอคอนมีชื่อที่ screen reader เข้าใจ และ error เชื่อมกับช่องที่ผิด
- [ ] UI-10 สถานะไม่สื่อด้วยสีเพียงอย่างเดียว; loading/error/success อ่านด้วย assistive technology ได้
- [ ] UI-11 ฟอนต์/รูป CDN โหลดไม่ได้ → ยังอ่านเนื้อหาและใช้ฟังก์ชันหลักได้
- [ ] UI-12 กดลิงก์ footer/contact/social → ไปปลายทางที่ตั้งใจ ไม่เหลือ `#` ที่ทำให้เข้าใจว่าฟังก์ชันพร้อมแล้ว
- [ ] UI-13 ตรวจข้อความไทย/อังกฤษ ชื่อเดือน วันที่ และคำอธิบายกติกา → ไม่ขัดกับกติกา backend หรือแสดงอักขระเสีย
- [ ] UI-14 Error boundary ปรากฏเมื่อจำลอง render error → มีทางลองใหม่/กลับหน้าปลอดภัยโดยไม่เผยรายละเอียดภายใน

## 21. Browser, Network และประสิทธิภาพ — P1/P2

- [ ] ENV-01 ทำ smoke flow บน Chrome/Edge/Firefox desktop → ให้ผลหลักเหมือนกัน
- [ ] ENV-02 ทำ smoke flow และกล้องบน Chrome Android/Safari iOS → จองและเช็กอินบนเครื่องจริงได้
- [ ] ENV-03 network ช้า/offline ระหว่างโหลดหน้า → มี feedback ไม่ loading ค้างและกลับมาใช้งานได้
- [ ] ENV-04 network หลุดระหว่าง mutation → ไม่แจ้งสำเร็จผิด และตรวจผลเดิมก่อน retry ได้
- [ ] ENV-05 background tab/เครื่อง sleep แล้วกลับมา → countdown/สถานะใช้เวลาปัจจุบัน ไม่ใช่เวลาที่หยุดไป
- [ ] ENV-06 session หมดอายุขณะเปิด dialog จองหรือ scanner → ออกจาก flow อย่างเหมาะสมและข้อมูลไม่ถูกส่งด้วยบัญชีผิด
- [ ] PERF-01 `[กำหนดเกณฑ์ก่อนรัน]` วัดเวลาโหลด Dashboard/availability และ p95 ของ API ที่จำนวนผู้ใช้ ______ → ไม่เกินเกณฑ์ ______ ms
- [ ] PERF-02 `[กำหนดเกณฑ์ก่อนรัน]` ทดสอบ concurrent booking จำนวน ______ requests → ไม่มี double booking และ error rate ไม่เกิน ______
- [ ] PERF-03 ใส่ประวัติ/ผู้ใช้จำนวนมากตามเป้าหมาย ______ records → หน้าและ API ยังตอบสนองโดยไม่โหลดเกินทรัพยากรที่กำหนด
- [ ] PERF-04 เปิด–ปิด Dashboard/scanner 20 รอบ → ไม่มี timer, request หรือกล้องสะสมผิดปกติ
- [ ] PERF-05 Cron กับผู้ใช้พร้อมกันตามโหลดเป้าหมาย → ไม่ deadlock และจัดการรายการครบภายในเกณฑ์ ______

## 22. Build, Deployment และ Regression — P1

- [ ] DEP-01 build frontend และ backend → สำเร็จบน runtime เวอร์ชันเดียวกับ deployment
- [ ] DEP-02 รัน production build และ refresh nested URL โดยตรง → routing/API rewrite ทำงาน
- [ ] DEP-03 ตรวจ BACKEND_URL, PORT และ reverse proxy → `/api` ส่งถึง backend ถูกตัวโดยไม่มี token หลุดไปปลายทางอื่น
- [ ] DEP-04 ถ้าใช้ Docker Compose ให้เริ่มใหม่ด้วย volume เดิม → service เชื่อมต่อกันและข้อมูลยังอยู่
- [ ] DEP-05 ทดสอบผ่าน domain/proxy/ngrok ที่ใช้งานจริง → API และกล้องผ่าน HTTPS ทำงาน ไม่มี warning page แทน JSON
- [ ] DEP-06 รัน automated tests ที่เกี่ยวข้อง → ผ่านและแนบ output; ไม่ถือว่า unit tests ผ่านแล้วเท่ากับ manual checklist ผ่านทั้งหมด
- [ ] DEP-07 หลัง deploy/rollback → ทำ SMK-01 ถึง SMK-08 ซ้ำด้วยข้อมูลทดสอบ
- [ ] DEP-08 ตรวจ console/network หลัง flow หลัก → ไม่มี unhandled exception, hydration error หรือ 401 loop

คำสั่งอ้างอิงสำหรับรันทดสอบที่มีอยู่ (รันจาก directory ที่ระบุ และใช้ฐานข้อมูลทดสอบเมื่อชุดทดสอบต้องใช้ DB):

```powershell
# frontend
node --test tests/*.test.mjs
node --test src/lib/booking-display.test.cjs
node node_modules/typescript/bin/tsc --noEmit
npm.cmd run lint
npm.cmd run build

# backend
npm.cmd test -- --runInBand
npm.cmd run test:e2e -- --runInBand
npm.cmd run build
```

## 23. จุดที่ต้องตรวจเป็นพิเศษจากการอ่านโค้ด

รายการต่อไปนี้เป็นความเสี่ยง/ความไม่สอดคล้องที่ต้องพิสูจน์ ยังไม่ใช่ผลทดสอบ:

- [ ] GAP-01 ยืนยันโควตา: backend นับ COMPLETED เป็นใช้โควตาทั้งวัน แต่ข้อความ error บางจุดสื่อว่าเสร็จแล้วจองใหม่ได้ → ตกลงและทำข้อความให้ตรง
- [ ] GAP-02 ตรวจ AVA-08: frontend บางจุดนับเฉพาะ PENDING/CHECKED_IN ว่าไม่ว่าง แต่ backend กัน COMPLETED ด้วย → ไม่ให้ UI หลอกว่าสนามว่าง
- [ ] GAP-03 ตรวจ QR-13: backend อนุญาตก่อนรอบ 15 นาที แต่ QR ผู้ดูแลรอเริ่มรอบ → ตกลง flow เช็กอินล่วงหน้า
- [ ] GAP-04 ตรวจ QR-06/07: scanner ใช้ parseInt และหยุดกล้องก่อนตรวจ QR → ไม่รับเลขปนข้อความและสแกนใหม่หลัง QR ผิดได้
- [ ] GAP-05 ตรวจ TIM-11: availability ที่ไม่ส่ง date ใช้วัน UTC → วันที่ต้องตรงกับนโยบายไทย
- [ ] GAP-06 ตรวจ BKG-12/API-03: courtId และ input ทุกชนิดต้องถูกตรวจที่ backend → ไม่พึ่ง validation ในฟอร์มเพียงอย่างเดียว
- [ ] GAP-07 ตรวจ SEC-01: endpoints ที่คืน entity พร้อม relations → ต้องไม่ติด password/hash ของ student/admin
- [ ] GAP-08 ตรวจ RACE-09: mutex อยู่ในหน่วยความจำ process → พิสูจน์ความถูกต้องบนจำนวน instance ที่ใช้งานจริง
- [ ] GAP-09 ตรวจ NTF-07/08 และ ADM-10: สถานะอ่าน notification/สถิติ/การอัปเดตต้องเป็นฟังก์ชันจริงตามที่ UI สื่อ

## บันทึกผลที่ไม่ผ่าน / Blocked / N/A

คัดลอกแถวเพิ่มได้ ใช้ Test ID ผูกกับ checklist และเก็บภาพ/วิดีโอ/response ที่ปกปิด token และข้อมูลส่วนตัวแล้ว

| Test ID | ผลจริง / ขั้นตอนทำซ้ำ | ผลที่คาดหวัง | สถานะ | Severity | Bug ID / หลักฐาน | ผู้ทดสอบ / วันที่ | ผล Retest |
| --- | --- | --- | --- | --- | --- | --- | --- |
| __________ | __________ | __________ | __________ | __________ | __________ | __________ | __________ |
| __________ | __________ | __________ | __________ | __________ | __________ | __________ | __________ |
| __________ | __________ | __________ | __________ | __________ | __________ | __________ | __________ |

## เกณฑ์ปิดรอบทดสอบ

- [ ] EXIT-01 P0 ผ่านทั้งหมด และไม่มี Critical/High ที่ยังไม่แก้หรือยังไม่ retest
- [ ] EXIT-02 P1/P2 ที่ไม่ผ่านหรือถูกเลื่อนมี Bug ID ผู้รับผิดชอบ และการยอมรับความเสี่ยงบันทึกไว้
- [ ] EXIT-03 รายการ `[ยืนยันกติกา]` ที่กระทบการปล่อยระบบได้ข้อสรุปและ expected result ชัดเจน
- [ ] EXIT-04 Smoke/Regression หลังแก้ไขรอบสุดท้ายผ่านใน environment เป้าหมาย
- [ ] EXIT-05 สรุปจำนวน Pass / Fail / Blocked / N/A และแนบหลักฐานครบ ไม่รวม N/A เป็น Pass

| สรุปรอบทดสอบ | จำนวน / รายละเอียด |
| --- | --- |
| Pass | ______ |
| Fail | ______ |
| Blocked / ยังไม่ทดสอบ | ______ |
| N/A พร้อมเหตุผล | ______ |
| พร้อมปล่อยระบบหรือไม่ / เหตุผล | ____________________ |
| ผู้ตรวจทาน / วันที่ | ____________________ |

ศัพท์ที่ใช้: **Expected result** = ผลที่คาดหวัง, **Actual result** = ผลที่เกิดจริง, **Boundary test** = ทดสอบค่าขอบเขต, **Regression test** = ทดสอบว่าการแก้ไขไม่ทำของเดิมพัง, **Retest** = ทดสอบซ้ำเพื่อยืนยันว่า bug ที่แก้หายแล้ว
